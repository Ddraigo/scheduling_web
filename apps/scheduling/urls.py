"""
URL routing for Scheduling app

⚠️ IMPORTANT: Do NOT include DefaultRouter here because it will catch custom endpoint names.
All ViewSet routes and custom endpoints are defined in config/urls.py
"""

from django.urls import path

app_name = 'scheduling'

# Empty urlpatterns - all routes defined in config/urls.py
urlpatterns = []
